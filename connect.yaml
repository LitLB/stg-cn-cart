  deployAs:
  - name: sync-catalog-talon-one
    applicationType: event
    endpoint: /syncCatalog
    scripts:
      postDeploy: npm install && npm run build && npm run connector:post-deploy
      preUndeploy: npm install && npm run build && npm run connector:pre-undeploy
    configuration:
      standardConfiguration:
        - key: CTP_REGION
          description: commercetools Composable Commerce API region
          required: true
        - key: TALON_ONE_CATALOG_ID
          description: talon.one catalog id for sync cart catalog item api
          required: true
      securedConfiguration:
        - key: CTP_PROJECT_KEY
          description: commercetools Composable Commerce project key
          required: true
        - key: CTP_CLIENT_ID
          description: commercetools Composable Commerce client ID
          required: true
        - key: CTP_CLIENT_SECRET
          description: commercetools Composable Commerce client secret
          required: true
        - key: CTP_SCOPE
          description: commercetools Composable Commerce client scope
          required: true
        - key: CONNECT_GCP_TOPIC_NAME_KEY
          description: connect Pub/Sub topic name
          required: true
        - key: CONNECT_GCP_PROJECT_ID_KEY
          description: connect Pub/Sub project id
          required: true
        - key: TALON_ONE_URL
          description: talon.one base url
          required: true
        - key: TALON_ONE_API_KEY
          description: talon.one api key
          required: true
        - key: TALON_ONE_API_KEY_PREFIX
          description: talon.one api key prefix
          required: true
  - name: event-cms-sync
    applicationType: event
    endpoint: /syncCMS
    scripts:
      postDeploy: npm install && npm run build && npm run connector:post-deploy
      preUndeploy: npm install && npm run build && npm run connector:pre-undeploy
    configuration:
      standardConfiguration:
        - key: CTP_REGION
          description: commercetools Composable Commerce API region
          required: true
        - key: TALON_ONE_CATALOG_ID
          description: talon.one catalog id for sync cart catalog item api
          required: true
      securedConfiguration:
        - key: CTP_PROJECT_KEY
          description: commercetools Composable Commerce project key
          required: true
        - key: CTP_CLIENT_ID
          description: commercetools Composable Commerce client ID
          required: true
        - key: CTP_CLIENT_SECRET
          description: commercetools Composable Commerce client secret
          required: true
        - key: CTP_SCOPE
          description: commercetools Composable Commerce client scope
          required: true
        - key: CONNECT_GCP_TOPIC_NAME_KEY
          description: connect Pub/Sub topic name
          required: true
        - key: CONNECT_GCP_PROJECT_ID_KEY
          description: connect Pub/Sub project id
          required: true
        - key: CS_BASE_URL
          description: content stack base url
          required: true
        - key: CS_API_KEY
          description: content stack api key
          required: true
        - key: CS_AUTHORIZATION
          description: content stack authorization
          required: true
        - key: CS_ENVIRONMENRS
          description: content stack env
          required: true
        - key: CS_BRANCH
          description: content stack branch
          required: true
        - key: CS_CONTENT_TYPE_UID
          description: content stack content type uid
          required: true